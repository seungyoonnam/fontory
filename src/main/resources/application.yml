spring:
  profiles:
    active: local  # ?? prod, dev ?? ??

---

spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:mariadb://ceprj.gachon.ac.kr:60023/db25123
    driver-class-name: org.mariadb.jdbc.Driver
    username: dbid251
    password: dbpass251

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect
    open-in-view: false

  logging:
    level:
      org.hibernate.SQL: debug

server:
  port: 60023

# Swagger ?? (??)
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method